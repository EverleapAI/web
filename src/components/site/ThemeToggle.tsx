"use client";

/**
 * Page theme is fixed to "white".
 * This control is intentionally a no-op so it never touches cookies
 * or localStorage and never changes <html data-theme>.
 */
export default function ThemeToggle() {
  return null;
}
